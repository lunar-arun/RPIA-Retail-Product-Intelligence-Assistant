pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
streamlit>=1.35
plotly>=5.20
python-dotenv>=1.0

# PDF report generation (Feature 5) — lightweight, no external service
reportlab>=4.0

# Optional local HF sentiment model (Feature 2).
# Only needed when RPIA_USE_HF_SENTIMENT_MODEL=true.
# The model (~260 MB) is downloaded once to the local HF cache and then
# runs fully offline — no API key, no network access at inference time.
# Install CPU-only torch to keep the download small:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# Then install transformers:
transformers>=4.40
torch>=2.0          # CPU-only build recommended; see comment above
